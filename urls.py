from django.urls import path
from . import views

urlpatterns = [
    # path('', views.YourListView.as_view(), name='list'),
    # path('<slug:slug>', views.YourDetailView.as_view(), name='detail'),
]
